#include<bits/stdc++.h>
using namespace std;

int main (void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    vector<int> dist(v, INFINITY)   // The distance vector stores the distance between the source node and the node, but also stores the info if it was or was not visited before



}