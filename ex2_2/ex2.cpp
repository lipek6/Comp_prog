#include<iostream>
#include<bits/stdc++.h>

int main (void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int N;
    while (true)
    {
        cin >> N;
        if (N == 0)
        {
            break;
        }
        else
        {
            int v;
            while (true)
            {
                cin >> v;
                if (v == 0)
                {
                    
                }

            }
        }
    }


}