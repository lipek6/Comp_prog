#include<bits/stdc++.h>
using namespace std;

int main (void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int fence_width; cin >> fence_width;
    vector<int> board_heigth(fence_width);
    int board_width = 1;
    
    for(int i = 0; i < fence_width; i++)
    {
        cin >> board_heigth[i];
        if()
    }





}