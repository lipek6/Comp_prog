#include<iostream>
#include<bits/stdc++.h>

int main (void)
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    std::stack<int>stk;

    int size = 1;
    int entry;
    while (true)
    {
        std::cin >> size;
        
        if (size == 0)
        {
            continue;
        }

        while (entry != 0)
        {
            std::cin >> entry;
        }



    }



}