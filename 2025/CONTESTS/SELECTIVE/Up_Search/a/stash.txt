#include<bits/stdc++.h>
using namespace std;

int main (void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int t; cin >> t;
    while(t--)
    {
        int n, k; cin >> n >> k;
        
        vector<int> a(n);
        vector<int> h(n);
        vector<int> lens(n,0);   lens[n-1] = 1;
        vector<long long> presum(n+1); presum[0] = 0;


        for(int i = 0; i < n; i++)
        {
            cin >> a[i];
            presum[i+1] = presum[i] + a[i]; 
        }

        for(int i = 0; i < n; i++) cin >> h[i];

        for(int i = n-2; i >= 0; i--)
        {
            if(h[i] % h[i+1] == 0)
                lens[i] = lens[i+1] + 1;
            else
                lens[i] = 1;
        }


        int max_length = 0;
        int length = 0;
        
        int l = 0;
        int r = 0;
        
        while(l < n)
        {
            if(preusm[r+1] - presum[l] > k)
            {
                max_length = max(length, max_length);
                l = r;
            }

            if(lens[])

        }








           

        for(int i = 0; i < n; i++) cout << lens[i] << " ";
        cout << endl;
        //cout << max_length << endl;
    }

}