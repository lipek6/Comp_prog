#include<bits/stdc++.h>
using namespace std;

int main (void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n; cin >> n;

    map<int, int> occurrences;
    vector<pair<int,int>> events;
    for(int i = 0; i < n; i++)
    {
        int in, out;
        cin >> in >> out;
        occurrences[in]++;
        occurrences[out]++;
        events.push_back({in, 1});
        events.push_back({out, -1});
    }
    sort(events.begin(), events.end());
    

    vector<int> presum(events.size() + 1);
    presum[0] = 0;

    for(int i = 0; i < events.size(); i++)
    {
        presum[i+1] = presum[i] + events[i].second;
        
        if(presum[i+1] >= 2 && events[i].first == events[i+1].first)
        {
            cout << "NO" << "\n";
            return 0;
        }
    }
    cout << "YES" << "\n";
}