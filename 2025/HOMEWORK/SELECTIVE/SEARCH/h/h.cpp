#include<bits/stdc++.h>
using namespace std;

// Maybe the local problem solves the global problem?
int main (void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n; cin >> n;
    vector<string> a(n);
    for(int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    
    vector<int> str_points(n, 0);
    for(int i = 0; i < n; i++)


}