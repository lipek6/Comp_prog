#include<bits/stdc++.h>
using namespace std;

typedef pair<int, int> ii;      // We will call a pair of ints 'ii'
typedef vector<ii> vii;         // We will call a vector of pairs of ints 'vii'         


int main(void)
{
    vector<vii> adj_list;
}